describe("Raises x to power n", function () {
    it("", function () {
        assert.equals(pow(5, 1), 5);
    });
    it("", function () {
        assert.equals(pow(5, 2), 25);
    });
    it("", function () {
        assert.equals(pow(5, 3), 125);
    });
})